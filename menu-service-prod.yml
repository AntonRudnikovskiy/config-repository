spring:
  datasource:
    # Используем переменные окружения для указания логинов, паролей и урлов
    username: user
    password: password
    url: jdbc:postgresql://postgres:15432/menu_service_db
  jpa:
    show_sql: false # Выключаем показ sql запросов
  flyway:
    user: user
    password: password
    url: jdbc:postgresql://postgres:15432/menu_service_db